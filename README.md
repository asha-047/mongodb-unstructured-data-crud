# mongodb-unstructured-data-crud

# Experiment 8: Using MongoDB to Store Unstructured Data

This project demonstrates how an e-learning platform can store unstructured data such as:
- Activity logs
- Chat messages
- Multimedia metadata (video tags, thumbnails)

We use **MongoDB** as a NoSQL database and perform CRUD operations.

---

## 📌 Steps Followed

### 1. Create Database and Collections
Database: `elearningDB`  
Collections: `activityLogs`, `mediaMetadata`

### 2. Insert Documents
Inserted activity logs and multimedia metadata.

### 3. Read Data
Queried all logs and fetched recent activities (last 24 hrs).

### 4. Update Data
Updated a user’s chat message.

### 5. Delete Data
Deleted old/unwanted records.

---

## 🚀 Run Locally

1. Install MongoDB on your system and ensure it’s running.
2. Clone this repo:
   ```bash
   git clone https://github.com/your-username/mongodb-unstructured-data-crud.git
   cd mongodb-unstructured-data-crud
